- name: 'upgrade packages'
  apt:
    update_cache: true
    upgrade: true
    autoclean: true

- name: 'install requirement packages'
  apt:
    name: '{{ requirements }}'

- name: 'rebooting...'
  reboot:
    msg: 'rebooting...